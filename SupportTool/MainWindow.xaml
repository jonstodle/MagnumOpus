<Window x:Class="SupportTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SupportTool"
        xmlns:controls="clr-namespace:SupportTool.Controls"
        mc:Ignorable="d"
        x:Name="ThisWindow"
        Icon="/Assets/Icon.ico"
        Title="Magnum Opus by Jon Stødle" MinHeight="200" MinWidth="200" Height="400" Width="900">

    <Grid Margin="2">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<Grid>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition/>
				<ColumnDefinition Width="Auto"/>
			</Grid.ColumnDefinitions>

			<Button x:Name="PasteButton" Content="Paste" Margin="2"/>

			<TextBox x:Name="SearchQueryTextBox" CharacterCasing="Upper" Grid.Column="1" Margin="2"/>

			<Button x:Name="HistoryButton" Content="History" Grid.Column="2" Margin="2">
				<Button.ContextMenu>
					<ContextMenu x:Name="HistoryButtonContextMenu">
						<ContextMenu.ItemContainerStyle>
							<Style TargetType="MenuItem">
								<EventSetter Event="Click" Handler="MenuItemClick"/>
							</Style>
						</ContextMenu.ItemContainerStyle>

						<ContextMenu.ItemTemplate>
							<DataTemplate>
								<TextBlock Text="{Binding}"/>
							</DataTemplate>
						</ContextMenu.ItemTemplate>
					</ContextMenu>
				</Button.ContextMenu>
			</Button>
		</Grid>

		<ListView x:Name="SearchResultsListView" Grid.Row="1" Margin="2">
			<ListView.View>
				<GridView>
					<GridView.Columns>
						<GridViewColumn Header="Name" DisplayMemberBinding="{Binding Properties[name][0]}" Width="300"/>
						<GridViewColumn Header="ID" DisplayMemberBinding="{Binding Properties[samaccountname][0]}" Width="80"/>
						<GridViewColumn Header="Path" DisplayMemberBinding="{Binding Path}" Width="200"/>
					</GridView.Columns>
				</GridView>
			</ListView.View>
		</ListView>

		<Grid Grid.Row="2">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition/>
				<ColumnDefinition Width="Auto"/>
			</Grid.ColumnDefinitions>

			<TextBlock x:Name="SearchResultsCountTextBox" Margin="2"/>

			<TextBlock Text="Showing first 1000 results" Opacity=".5" Grid.Column="2" Margin="2"/>
		</Grid>
    </Grid>
</Window>
